# --- Measured CI cost (dogfood-swarm SKILL item 8) --------------------------
# 10 billable min per push across 8 jobs (6.1 min actual wall time),
# measured 2026-07-04 from run 28722872805. Most expensive jobs:
#     2 min  Org URL sanity check  (1.1 min wall)
#     2 min  Python 3.12 on ubuntu-latest  (1.9 min wall)
#     2 min  Integration Tests  (1.4 min wall)
# GitHub bills per JOB, rounded UP to the minute, x2 Windows / x10 macOS, so
# billable minutes exceed wall time - many short jobs cost more than one long
# one. This number is measured, never estimated; re-measure after any change:
#   gh api repos/mcp-tool-shop-org/tool-compass/actions/runs/<id>/jobs
# ---------------------------------------------------------------------------
# Tool Compass CI Pipeline
# Runs tests on pushes and PRs that touch code or config.
# Also handles GitHub Pages deploy for site/** changes on main (folded in from
# the former pages.yml per the two-workflow-per-repo hard rule).

name: CI

on:
  push:
    branches: [main]
    paths:
      - '*.py'
      - 'tests/**'
      - 'scripts/**'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - 'Dockerfile'
      - 'site/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
    paths:
      - '*.py'
      - 'tests/**'
      - 'scripts/**'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - 'Dockerfile'
      - 'site/**'
      - '.github/workflows/**'
  # TST-FT-002: nightly-fuzz job runs Hypothesis in `nightly` profile
  # Mon/Wed/Fri at 09:00 UTC. Kept in this file to preserve the
  # two-workflow-per-repo limit.
  schedule:
    - cron: '0 9 * * 1,3,5'
  workflow_dispatch:

# NOTE: concurrency is intentionally NOT set at the workflow level.
# A workflow-level cancel-in-progress would cancel the whole run when a
# new push lands on the same ref — including the pages-deploy job that
# carries its own concurrency: pages / cancel-in-progress: false. The
# job-level setting cannot override workflow-level cancellation because
# the entire run is cancelled before per-job concurrency takes effect.
# Each test/lint/integration/docker job below carries its own
# concurrency block scoped to PR refs only (push events on main are
# never cancelled). The pages-build / pages-deploy jobs keep their
# dedicated concurrency: pages group with cancel-in-progress: false.

# Default least-privilege; individual jobs that need more elevate explicitly.
permissions:
  contents: read

jobs:
  lint:
    name: Org URL sanity check
    runs-on: ubuntu-latest
    # CT-B-011: lint is a sub-30-second shell check; 5 min is generous.
    timeout-minutes: 5
    # PR-scoped concurrency: a newer push to the same PR cancels the
    # older run, but pushes to main never cancel themselves.
    concurrency:
      group: lint-${{ github.event.pull_request.number || github.run_id }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Check for stale org/user URLs
        run: bash scripts/check-org-urls.sh

      - name: Set up Python (pre-commit)
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: '3.12'

      # CT-B-018: run the pre-commit hooks (ruff format + ruff check +
      # standard hygiene + gitleaks). Same hooks the contributor runs
      # locally; this is the CI-side enforcement. Soft-fail for the first
      # cycle to surface formatting drift without blocking PRs while
      # contributors install pre-commit locally.
      # TODO(swarm): flip continue-on-error to false after one full release
      # cycle so the pre-commit gate becomes blocking.
      - name: Run pre-commit hooks
        continue-on-error: true
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

      # CDS-FT-001: SCORECARD drift guard — now BLOCKING. The dogfood-swarm v3
      # pass fixed CIDOCS-01 (regenerate-scorecard.sh forces NO_COLOR + strips
      # ANSI), so `--check` diff-cleans deterministically in non-TTY CI. The
      # committed SCORECARD was verified in sync at flip time.
      - name: Verify SCORECARD is in sync
        run: |
          if [ -f Makefile ] && grep -q "^verify-scorecard:" Makefile; then
            make verify-scorecard
          else
            echo "Makefile verify-scorecard target missing — skipping"
          fi

      # CT-B-008 cross-domain: verify that /metrics emits the expected
      # Four Golden Signals surface. Soft-fail because the saturation
      # gauges land in BE-B-002 (backend domain); flip to fatal once that
      # work merges.
      - name: Verify metrics surface (Four Golden Signals)
        continue-on-error: true
        run: |
          if [ -f Makefile ] && grep -q "^verify-metrics:" Makefile; then
            python -m pip install --upgrade pip
            pip install -r requirements.txt || true
            make verify-metrics || true
          else
            echo "Makefile verify-metrics target missing — skipping"
          fi

  test:
    name: Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # CT-B-011: per-matrix-cell budget. pytest has its own 30s per-test cap
    # (pyproject.toml [tool.pytest.ini_options]); 15 min envelope covers
    # install + coverage + pip-audit with headroom for a slow runner.
    timeout-minutes: 15
    # PR-scoped concurrency (see workflow-top NOTE).
    concurrency:
      group: test-${{ matrix.python-version }}-${{ github.event.pull_request.number || github.run_id }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        # One version, because one is all this repo can currently test.
        #
        # 3.13 was dropped earlier: hnswlib 0.8.0's cp313 wheel SIGILLs on
        # GitHub ubuntu-latest runners (AVX instruction gap).
        #
        # 3.11 is dropped here 2026-09-08 for the SAME reason, now reaching
        # numpy: pytest died at collection with "Illegal instruction (core
        # dumped)" importing numpy._core._multiarray_umath. Reproducible across
        # a rerun, and 3.12 passed the same commit both times. Version pinning
        # would not have helped -- the last green run used the SAME numpy 2.1.3
        # on the SAME runner image, so the wheel's CPU dispatch, not its
        # version, is what changed underneath.
        #
        # READ THIS AS A RUNNER LIMITATION, NOT A LANGUAGE ONE. Nothing here
        # needs 3.12 syntax; 3.11 is dropped because prebuilt wheels crash on
        # GitHub's ubuntu-latest CPUs, not because the code stopped supporting
        # it. requires-python was raised to >=3.12 in the same commit so the
        # package stops claiming an interpreter no gate exercises -- a green
        # badge over an untested runtime is worse than a narrow matrix.
        # Reinstating 3.11 is a deliberate decision with its own test strategy
        # (a self-hosted runner, or a source build of hnswlib/numpy).
        python-version: ['3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: 'requirements*.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run tests
        run: |
          pytest -v --tb=short -m "not integration" \
            --junitxml=junit-${{ matrix.python-version }}.xml

      - name: Upload JUnit report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: junit-reports-${{ matrix.python-version }}
          path: junit-${{ matrix.python-version }}.xml
          if-no-files-found: warn
          # CT-B-012: JUnit reports are short-lived diagnostic data — only
          # useful for the most recent few runs. Default 90d is wasted.
          retention-days: 14

      - name: Run tests with coverage
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        run: |
          pytest --cov=. --cov-report=xml --cov-report=term -m "not integration"

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 # v5
        with:
          file: coverage.xml
          fail_ci_if_error: false

      - name: Run pip-audit (report only)
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        run: |
          pip-audit -r requirements.txt --format json --output pip-audit-report.json || true
        # Non-blocking: full output captured as artifact for reviewers.

      - name: Upload pip-audit report
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: pip-audit-report
          path: pip-audit-report.json
          if-no-files-found: ignore
          # CT-B-012: CVE diagnostic snapshot — 14d aligns with the JUnit
          # retention and is plenty of window for incident response.
          retention-days: 14

      # Now BLOCKING. The dogfood-swarm v3 pass reviewed the CVE baseline:
      # `pip-audit -r requirements.txt` reports no known vulnerabilities. The
      # report-only artifact step above still runs for the full JSON snapshot.
      - name: Audit dependencies for vulnerabilities
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        run: |
          pip-audit -r requirements.txt

  # TST-FT-002: nightly Hypothesis fuzzing. Runs on schedule (Mon/Wed/Fri 09:00
  # UTC) or manually via workflow_dispatch. Never runs on push/PR — nightly-only.
  # On failure, opens a tracking issue via actions/github-script.
  nightly-fuzz:
    name: Nightly Hypothesis fuzz
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    # CT-B-011: Hypothesis nightly profile is deliberately slow — 45 min cap
    # is wider than the test job because fuzz examples are unbounded.
    timeout-minutes: 45
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'requirements*.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run Hypothesis (nightly profile)
        id: fuzz
        run: |
          HYPOTHESIS_PROFILE=nightly pytest -m "not integration"

      - name: Open tracking issue on failure
        if: failure() && steps.fuzz.conclusion == 'failure'
        # SHA-pinned per SLSA L3 supply-chain hygiene (CT-B-001). nightly-fuzz
        # carries issues:write, so a compromised v7 mutable tag would inherit
        # issue-creation capability. Bump via dependabot's github-actions sweep.
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const title = `[nightly-fuzz] Hypothesis failures on ${context.sha}`;
            const body = [
              `Nightly Hypothesis run failed on commit \`${context.sha}\`.`,
              ``,
              `- Workflow: ${context.workflow}`,
              `- Run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              `- Triggered by: ${context.eventName}`,
              ``,
              `Investigate and either fix the underlying flake or narrow the strategy.`,
            ].join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: ['nightly-fuzz', 'bug'],
            });

  # Integration tests (requires Ollama)
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: test  # Only run after unit tests pass
    # CT-B-011: Ollama install + nomic-embed-text pull + integration suite
    # dominates; 25 min envelope covers the cold-start pull path.
    timeout-minutes: 25
    # PR-scoped concurrency (see workflow-top NOTE).
    concurrency:
      group: integration-${{ github.event.pull_request.number || github.run_id }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'requirements*.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Install pinned Ollama
        # Pinned to a specific Ollama release instead of piping `curl | sh`.
        # Ollama's GitHub releases don't ship per-file .sha256 siblings, so
        # we try to fetch one and fall back to a SHA-less pin. TODO(swarm):
        # switch to attestation verification once Ollama publishes SLSA
        # provenance, or hard-pin the known-good SHA-256 inline.
        run: |
          set -euo pipefail
          OLLAMA_VERSION="v0.4.7"
          BASE_URL="https://github.com/ollama/ollama/releases/download/${OLLAMA_VERSION}"
          TARBALL="ollama-linux-amd64.tgz"
          curl -fsSL "${BASE_URL}/${TARBALL}" -o "${TARBALL}"
          if curl -fsSL "${BASE_URL}/${TARBALL}.sha256" -o "${TARBALL}.sha256" 2>/dev/null; then
            sha256sum -c "${TARBALL}.sha256"
          else
            echo "::warning::Ollama ${OLLAMA_VERSION} release has no .sha256 file; proceeding without checksum verification (known upstream gap)."
          fi
          sudo tar -C /usr/local -xzf "${TARBALL}"
          rm -f "${TARBALL}" "${TARBALL}.sha256"
          ollama --version
          ollama serve &
          # Wait for the daemon to accept connections instead of a blind sleep.
          for _ in $(seq 1 30); do
            if curl -sf http://localhost:11434/api/version > /dev/null; then
              break
            fi
            sleep 1
          done
          ollama pull nomic-embed-text

      - name: Run integration tests
        run: |
          pytest -v -m integration
        continue-on-error: true  # Integration tests may be flaky

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: test
    # CT-B-011: docker buildx + gha cache; 20 min covers a cold-cache build.
    timeout-minutes: 20
    # PR-scoped concurrency (see workflow-top NOTE).
    concurrency:
      group: docker-${{ github.event.pull_request.number || github.run_id }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Build Docker image
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7.1.0
        with:
          context: .
          push: false
          tags: tool-compass:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===========================================================================
  # GitHub Pages build + deploy (folded in from former pages.yml)
  # Only runs on pushes to main — never on PRs. Deploy job carries the
  # elevated permissions; build job stays read-only.
  # ===========================================================================
  pages-build:
    name: Build site
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    # CT-B-011: Starlight npm ci + Astro build; 10 min is generous.
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 22

      # CT-B-016: read repo-level Pages configuration so the build is not
      # silently dependent on Settings → Pages UI state. configure-pages
      # is idempotent and exposes the base path to downstream steps.
      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Install site dependencies
        working-directory: site
        run: npm ci

      - name: Build site
        working-directory: site
        run: npm run build

      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site/dist

  pages-deploy:
    name: Deploy to GitHub Pages
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: pages-build
    runs-on: ubuntu-latest
    # CT-B-011: deploy is a metadata operation against the Pages backend;
    # 5 min is the fail-fast budget.
    timeout-minutes: 5
    # Dedicated concurrency group so a long deploy is never cancelled by a
    # newer CI run (CDS-A-012).
    concurrency:
      group: pages
      cancel-in-progress: false
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
